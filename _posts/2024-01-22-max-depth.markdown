---
title:  "About Max Depth"
show_date: true
comments: true
layout: single
---

I've changed `max_depth` parameter, range from 3 to 13.

To be brief, as same with the penalization parameters, the `max_depth` parameter were effective for `default XGBoost` but not for the `XGBoost with random forest`.<br>

I attached the RMSE graph in each case below.<br>

**[`max_depth` ranges from 3 to 13]**

**1. Default XGBoost**
{% include plotly/RMSE_of_BT_max_depth_change.html %}


**2. XGBoost with Random Forest**
{% include plotly/RMSE_of_BTRF_max_depth_change.html %}

The below plot is the mean of RMSE for the whole test period.
![image](/assets/result_images/rmse_max_depth.png){: .align-center}
